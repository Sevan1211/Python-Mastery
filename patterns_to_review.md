# Patterns to Review (Spaced Repetition)

## Urgent
- Strings Level 5 drills: `longest_unique_substring`, `group_anagrams`, `is_valid_parens` — not yet attempted
- Primitive Data Types: 9 remaining practice drills (type conversions, truthy/falsy, float precision, None checks)
- Lists: all 20 practice drills pending — need hands-on practice with traversal, mutation, two-pointer, sliding window patterns

## Medium
- Tuple unpacking patterns (star unpacking, _ ignoring, swap idiom)
- Tuple hashability rules — when tuples can/can't be dict keys
- String `.rfind()` vs `.find()` — used in extract_domain drill
- String slicing with negative indices and step

## Maintenance
- Basic string methods (upper, lower, strip, split, replace, find, count)
- f-string formatting
- String indexing and iteration
